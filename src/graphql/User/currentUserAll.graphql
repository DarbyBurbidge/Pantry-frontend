query CurrentUserAll {
	currentUser {
		_id
		email
		tokenVersion
		items {
			_id
			itemName
			quantity
			expiration
			tags
			favorite
		}
		shoppingList {
			_id
			items {
				_id
				itemName
				quantity
				expiration
				tags
				favorite
			}
		}
	}
}