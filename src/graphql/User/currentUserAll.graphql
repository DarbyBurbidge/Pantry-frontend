query CurrentUserAll {
	currentUser {
		_id
		email
		tokenVersion
		items {
			_id
			itemName
			quantity
			expiration
			favorite
			tags	
		}
		shoppingList {
			_id
			items {
				_id
				itemName
				quantity
				expiration
				favorite
				tags
			}
		}
	}
}